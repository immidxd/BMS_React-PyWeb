-- products.technologyid виведено з ужитку: технології тепер many-to-many.
--
-- Перехід завершено попереднім комітом: читання йде агрегатом із
-- product_technologies, запис розкладає CSV на атоми, парсер веде звʼязки в
-- обох гілках, а writeback отримує вже зібраний рядок. Скаляр не читає й не
-- пише НІХТО — перевірено grep'ом по backend і frontend.
--
-- ЧОМУ ЗНАЧЕННЯ ЗАНУЛЯЮТЬСЯ, А НЕ ЛИШАЮТЬСЯ «ПРО ВСЯК ВИПАДОК»
-- ────────────────────────────────────────────────────────────
-- Колонка, яку ніхто не оновлює, але в якій лежать дані, — це пастка: наступний
-- запит до неї поверне зріз на день переходу й виглядатиме правдоподібно.
-- Порожня колонка такої омани не створює. Самі дані не втрачені: вони живуть у
-- product_technologies (175 товарів, 273 звʼязки) і у бекапі бекфілу.
--
-- ЧОМУ КОЛОНКА НЕ ВИДАЛЯЄТЬСЯ ЗАРАЗ
-- ─────────────────────────────────
-- DROP COLUMN незворотний, а колонка ще присутня в моделі й у схемах
-- (ProductBase/Update/List). Прибирати її звідти — окрема правка API, яку
-- варто робити свідомо. Поки що колонка лишається порожньою і позначеною.
--
-- ⚠️ Історичний скрипт standardize_brands_2026_08_21.py читає й пише цю
-- колонку. Він одноразовий і вже відпрацьований — повторно не запускати.

UPDATE products SET technologyid = NULL WHERE technologyid IS NOT NULL;

COMMENT ON COLUMN products.technologyid IS
    'DEPRECATED (2026-09-04). Технології живуть у product_technologies. Колонка не читається й не оновлюється; лишена порожньою до окремої правки API.';

COMMENT ON TABLE product_technologies IS
    'Технології товару (many-to-many) — ЄДИНЕ джерело правди. ord = порядок у комірці «Технології» Журналу.';
