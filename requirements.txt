# FastAPI and server
fastapi==0.115.12
uvicorn==0.34.0
starlette==0.46.1
# WebSocket-реалізація для uvicorn — без неї WS-ендпоінти падають (WinError 10054
# у frozen-білді), а на них тримається live прогрес-бар парсингу.
websockets==16.0

# Data validation
pydantic==2.11.1
pydantic-core==2.33.0

# Database
sqlalchemy==2.0.40
alembic==1.15.2
psycopg2-binary==2.9.10

# Desktop application
pywebview==5.4

# Google Sheets integration
gspread==6.2.0
google-auth==2.38.0
google-auth-oauthlib==1.2.1
oauth2client==4.1.3
# Google Drive API — backend/services/product_images_drive.py (фото товару).
# Раніше ставився руками на dev-Mac, але не був у requirements → впав би у frozen-білді.
google-api-python-client==2.198.0
# ISO-довідники країн — backend/scripts/googlesheets_pars.py (так само бракувало).
pycountry==26.2.16

# Networking
requests==2.32.3
aiohttp==3.13.5
httpx==0.28.1
anyio==4.9.0

# Cloudflare R2 (S3-compatible storage for product photos)
boto3==1.43.49

# Utilities
python-dotenv==1.1.0
python-multipart==0.0.28
Jinja2==3.1.6
MarkupSafe==3.0.3

# Optional: For scraping/parsing functionality
beautifulsoup4==4.14.3
lxml==6.1.0
# (selenium / webdriver-manager прибрано — не використовувались у коді, роздували інсталятор)

# Image processing (фото товару → WebP-майстер; HEIC = формат iPhone)
Pillow==12.2.0
# Шрифти пристрою для майстерні: метадані гарнітур і витяг з колекцій .ttc
fonttools==4.63.0
pillow-heif==1.4.0

# Telegram integration
telethon==1.36.0

# Background tasks / scheduling
APScheduler==3.10.4

# Testing
pytest==9.0.3
pytest-asyncio==1.3.0
